{
  "name": "poc",
  "version": "1.0.0",
  "description": "This runs the **real, unmodified** provider handlers from the installed `nuxt-auth-utils` package against a mock OAuth server, and demonstrates that the `google` handler accepts an attacker-forged callback (stolen `code`, attacker `state`, **no valid `nuxt-auth-state` cookie**) while `github` rejects it.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "h3": "^2.0.1-rc.26",
    "nuxt-auth-utils": "^0.5.29",
    "ofetch": "^1.5.1"
  }
}
